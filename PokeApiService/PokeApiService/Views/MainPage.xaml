<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vmodel="clr-namespace:PokeApiService.ViewModels"
             x:Class="PokeApiService.MainPage">

    <ContentPage.BindingContext>
        <vmodel:PokemonsVM></vmodel:PokemonsVM>
    </ContentPage.BindingContext>

    <VerticalStackLayout>
        <ListView ItemsSource="{Binding ListadoPokemons}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame BorderColor="LightGray" CornerRadius="5" Padding="10" Margin="5">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="{Binding name}" FontSize="Medium" FontAttributes="Bold" />
                                <Label Text="{Binding url}" FontSize="Medium" />
                            </VerticalStackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Mostrar Pokemons" Command="{Binding MostrarListado}"></Button>
    </VerticalStackLayout>

</ContentPage>
